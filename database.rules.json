{
  "rules": {
    "session": {
      ".read": "auth != null",

      "name":      { ".write": "(auth.token.email === 'florian.cheval@gmail.com' || auth.token.email === 'test-admin@votrix.dev')" },
      "phase":     { ".write": "(auth.token.email === 'florian.cheval@gmail.com' || auth.token.email === 'test-admin@votrix.dev')" },
      "shortUrl":  { ".write": "(auth.token.email === 'florian.cheval@gmail.com' || auth.token.email === 'test-admin@votrix.dev')" },
      "sessionId": { ".write": "(auth.token.email === 'florian.cheval@gmail.com' || auth.token.email === 'test-admin@votrix.dev')" },

      "participants": {
        ".write": "auth != null",
        ".validate": "newData.isNumber() && newData.val() >= 0"
      },
      "votedCount": {
        ".write": "auth != null",
        ".validate": "newData.isNumber() && newData.val() >= 0"
      },

      "ideas": {
        ".write": "(auth.token.email === 'florian.cheval@gmail.com' || auth.token.email === 'test-admin@votrix.dev')",
        "$ideaId": {
          ".write": "auth != null && (!data.exists() || data.child('authorUid').val() === auth.uid)",
          ".validate": "newData.hasChildren(['id','title','author','authorUid']) && newData.child('title').isString() && newData.child('title').val().length <= 40 && newData.child('author').isString()",
          "votes": {
            ".write": "auth != null",
            ".validate": "newData.isNumber() && newData.val() >= 0"
          }
        }
      },

      "ballots": {
        ".read": "(auth.token.email === 'florian.cheval@gmail.com' || auth.token.email === 'test-admin@votrix.dev')",
        ".write": "(auth.token.email === 'florian.cheval@gmail.com' || auth.token.email === 'test-admin@votrix.dev')",
        "$uid": {
          ".read": "auth.uid === $uid",
          ".write": "auth.uid === $uid && !data.exists()",
          "$ideaId": {
            ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100"
          }
        }
      }
    },

    "presence": {
      ".read": "auth != null",
      ".write": "(auth.token.email === 'florian.cheval@gmail.com' || auth.token.email === 'test-admin@votrix.dev')",
      "$uid": {
        ".write": "auth.uid === $uid"
      }
    }
  }
}
